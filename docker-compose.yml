version: "3.8"
services:
  autopilot:
    build: .
    ports:
      - "${DASHBOARD_PORT:-8080}:8080"
    env_file:
      - .env
    volumes:
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
